import Navbar from "@/components/ui/navbar";
import React from "react";

const Header: React.FC = () => {
  return (
    <header className="border-b mb-5  bg-gray-800 text-white">
      <Navbar />
    </header>
  );
};

export default Header;
